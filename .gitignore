# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# env — jangan pernah commit credential asli
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor/OS
.DS_Store
.vscode/
.idea/

# test coverage
coverage/

# dokumen karyawan yang diupload lokal (dev) — di production disimpan di
# volume Docker `employee_documents`, tidak pernah masuk git
/uploads/

# hasil scripts/backup.sh — dump database berisi data sensitif, tidak
# pernah masuk git
/backups/
